<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record model="ir.actions.act_window" id="action_convention_facture">
        <field name="name">Factures</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">facture</field>
        <field name="view_mode">tree,form</field>
        <field name="view_id" ref="facture_tree_view"/>
        <field name="domain">[('conv_id', '=', active_id)]</field>
        <field name="context">{"default_conv_id": active_id}</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    Créer une nouvelle facture.
                </p><p>

                </p><p>

                </p>
            </field>
    </record>

 <record id="convention_facture_form_view" model="ir.ui.view">
        <field name="name">convention.facture.form</field>
        <field name="model">convention</field>
        <field name="inherit_id" ref="engagement.convention_form_view"/>
        <field name="arch" type="xml">
            <div name="button_box" position="inside">
                <button class="oe_stat_button" type="action" name="%(action_convention_facture)d" invisible="state in ('active', )" icon="fa-file-text">
                    <field string="Factures" name="nbr_facture" widget="statinfo"/>
                </button>
            </div>
        </field>
    </record>
</odoo>